<!DOCTYPE html>
<html>
   <head>
      <title>RailsBaBackend</title>
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
      <%= stylesheet_link_tag    'application', media: 'all' %>
   </head>
   <body>
      <header class="nav-banner">
      <nav class="navbar container navbar-expand-sm navbar-light bg-danger">
         <div class="container">
            <a class="navbar-brand  text-light" href="#">Burning Airlines </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse " id="navbarSupportedContent">
               <ul class="navbar-nav me-auto mb-2 mb-lg-0 ">
                  <% if @current_user && @current_user.admin %>
                  <li class="nav-item">
                     <%= link_to 'Airlpanes', airplanes_path, :class=> 'nav-link text-light' %>
                  </li>
                  <li class="nav-item  ">
                     <%= link_to 'Flights', flights_path, :class=> 'nav-link text-light'%>
                  </li>
                  <% end %>
                  <li class="nav-item ">
                     <a class="nav-link   text-light" href="#" id="navbarDropdown" role="button"  aria-expanded="false">
                     Reservation
                     </a>
                  </li>
               </ul>
               <ul class="navbar-nav" >
                  <% if @current_user.present? %>
                  <li class="nav-item">
                     <%= link_to "#{@current_user.name}", edit_user_path(@current_user.id), :class=> 'nav-link text-light' %> 
                  </li>
                  <li class="nav-item nav-link text-light">|</li>
                  <li class="nav-item">
                     <%= link_to 'Logout', login_path, method: 'delete', :class=> 'nav-link text-light' %>
                  </li>
                  <% else %>
                  <li class="nav-item text-light">
                     <%= link_to 'Login', login_path , :class=> 'nav-link'%>
                  </li>
                  <% end %>
               </ul>
            </div>
         </div>
      </nav>
      <div class="container">
         <%= yield %>
      </div>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
      <%= javascript_include_tag 'application' %>
   </body>
</html>